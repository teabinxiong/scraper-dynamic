# Generated by generate-proxy-configurations script.

version: '3'

services:
  tor-A1:
    container_name: tor-A1
    image: 'pickapp/tor-proxy:latest'
    ports:
      - '9990:8888'
    environment:
      - 'IP_CHANGE_INTERVAL=30'
    restart: always
  tor-A2:
    container_name: tor-A2
    image: 'pickapp/tor-proxy:latest'
    ports:
      - '9991:8888'
    environment:
      - 'IP_CHANGE_INTERVAL=30'
    restart: always
  tor-A3:
    container_name: tor-A3
    image: 'pickapp/tor-proxy:latest'
    ports:
      - '9992:8888'
    environment:
      - 'IP_CHANGE_INTERVAL=30'
    restart: always
  tor-A4:
    container_name: tor-A4
    image: 'pickapp/tor-proxy:latest'
    ports:
      - '9993:8888'
    environment:
      - 'IP_CHANGE_INTERVAL=30'
    restart: always
